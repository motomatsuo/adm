# Dockerfile para o Backend (standalone)
# Use este arquivo quando precisar fazer deploy apenas do backend
# Renomeie para Dockerfile antes do deploy, ou configure no Easypanel

FROM node:18-alpine

WORKDIR /app

# Copiar arquivos de dependências
COPY backend/package*.json ./
RUN npm ci --only=production

# Copiar código fonte
COPY backend/ ./

# Build TypeScript
RUN npm run build

EXPOSE 3001

# Usar node para rodar o servidor
CMD ["node", "dist/index.js"]
